import React from "react";
import ContainerPage from "./ContainerPage";
import "remixicon/fonts/remixicon.css";

const Saved = () => {
  const thisPage = "Saved Videos";

  return <ContainerPage props={thisPage} />;
};

export default Saved;
